Pending plugin release changes since v1.1.24:

Use only this section as input for the next plugin release notes.

- Added a post-agent-only warning badge next to the Agent Editor title so users know post-processing agents are harder to tune and more prompt-sensitive than pre-agents.
  - Files touched: `risu_agents.js`
  - Tests/checks: `git diff --check`; Node-backed REPL syntax compile of `risu_agents.js`
  - Could not run: `node --check risu_agents.js` because `node` is not available on PATH in this shell
  - Commit: `bcf24bd Add post-agent editor warning badge`
  - Release status: version bump, tag, push, and GitHub Release intentionally skipped pending user approval

Completed non-release project work:

These entries are retained for project handoff context and are not plugin release-note inputs.

- Added a draft Markdown prompt document that separates the Skyrim post-processing agent's customizable system prompt, read-only hardcoded prompt reference, and customizable output instruction so the editable prompt text can be discussed alongside the plugin-appended protocol. Cleaned up the customizable system prompt heading structure without removing prompt content, renamed the player sheet section to `Player Status Window` under a shared `Status Windows` section, normalized the editable prompt text to English, moved output execution rules into the customizable output instruction, added narrator examples plus output rules for explicit resource recovery over time/rest/sleep, split common player/follower status window component descriptions into `Shared Status Window Rules`, moved player-only `Quests` and `Followers` descriptions into the `Player Status Window` section, added an explicit `Processing order` to the output instruction with image commands and narrator quotes grouped as inline additions, expanded narrator numerical examples so player/follower resource, skill, EXP, level-up, recovery, and compound progression changes identify the affected character, instructed narrator quotes to match the language of `Current Response`, and strengthened narrator placement rules so numerical reports are inserted at their triggering moment instead of being collected at the end.
  - Files touched: `docs/skyrim_post_agent_prompt_draft.md`
  - Tests/checks: `git diff --check`; `sed -n '1,260p' docs/skyrim_post_agent_prompt_draft.md`; `sed -n '50,125p' docs/skyrim_post_agent_prompt_draft.md`; `sed -n '66,112p' docs/skyrim_post_agent_prompt_draft.md`; `sed -n '90,155p' docs/skyrim_post_agent_prompt_draft.md`; `sed -n '99,135p' docs/skyrim_post_agent_prompt_draft.md`; `sed -n '190,230p' docs/skyrim_post_agent_prompt_draft.md`; `sed -n '204,218p' docs/skyrim_post_agent_prompt_draft.md`; `sed -n '216,248p' docs/skyrim_post_agent_prompt_draft.md`; `rg -n "Character Sheet|Journal Interface|Player Character Status|Status Windows|Player Status Window|Follower Status Window" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "[가-힣]" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "placed|Before a character|must comment|Current Response|Output Instruction|Player Status Window|Follower Status Window" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "Recovery|recovery|rest|sleep|Resource Use/Resource Change|Stamina Recovery|Magicka Recovery|Multiple Resource" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "Shared Status Window Rules|Player Status Window|Follower Status Window|Component Description|Player Status Window only|Follower Status Window" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "Player-only Component Description|Quests:|Followers:" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "Processing order|Apply inline additions|image command directly before|Output only the full revised Current Response" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "affected character|causal order|Follower Stamina Recovery|User Combat Progression Chain|Follower Combat Progression Chain|User Speech Skill Increase|Follower Speech Skill Increase|Follower Destruction Skill Increase" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "Match narrator quote language|Narrator quotes:|Current Response" docs/skyrim_post_agent_prompt_draft.md`; `rg -n "Do not collect narrator quotes|immediately after|same immediate event|different moments|Inline Placement Example|end of Current Response" docs/skyrim_post_agent_prompt_draft.md`
  - Commits: `b09524f Add Skyrim post-agent prompt draft`; `aeb02b5 Limit Skyrim prompt draft to customizable text`; `bc40991 Add read-only hardcoded post-agent prompt reference`; `0f57951 Clean up Skyrim prompt draft structure`; `1ae131f Rename player status prompt section`; `6e7523d Move post-agent output rules into instruction`; `e187cfd Add resource recovery narrator examples`; `3f56b43 Split shared status window rules`; `7fa0c15 Add output instruction processing order`; `7449299 Move player-only status fields`; `1f4ab21 Expand narrator numerical examples`; `4fadc49 Match narrator language to current response`; `6bcb1fa Prevent end-stacked narrator reports`
  - Release status: not applicable; this standalone prompt draft is not part of the plugin release

- Updated the local RisuAI Plugin API v3.0 references through upstream `v2026.6.114`, documented `getCurrentLorebookEntries()` as a raw candidate API rather than an exact active-lorebook API, and added root-level `DEFERRED.md` as the shared tracker for postponed project work. The first deferred item records the future official lorebook candidate API migration while leaving `risu_agents.js` unchanged.
  - Files touched: `DEFERRED.md`; `docs/risuai/README.md`; `docs/risuai/plugins.md`; `docs/risuai/types/risuai.d.ts`
  - Tests/checks: `git diff --check`; `git diff --cached --check`; exact `diff -u` comparison of the local DTS against upstream `v2026.6.114`; `rg` verification for raw candidate, activation, recursive scanning, and token-budget limitations; `git diff -- risu_agents.js` confirmed no runtime changes
  - Commit: `bf968fb Document current lorebook entries API`
  - Release status: not applicable; these internal reference and tracking files are not part of the plugin release
